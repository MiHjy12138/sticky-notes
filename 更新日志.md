# 便签 更新日志

Win7 风格便签（PowerShell + WPF），数据保存在 `%APPDATA%\StickyNotes\`（可用环境变量 `STICKYNOTE_DATA_DIR` 覆盖）。

## v1.6（2026-08-14）
- **新增：右键菜单「删除线」**——选中文字加/取消删除线（再点一次取消），无选中时切换全文
- 底层 TextBox → RichTextBox；纯文本存储用 `~~文字~~` 标记删除线，.txt 兼容、历史窗口/旧便签零影响
- 修复：文字区域右键被系统"剪切/复制/粘贴"菜单拦截（v1.1 起隐藏 bug），现在显式绑定自定义菜单
- 修复：PS 5.1 下 `[TextElement]::TextDecorationsProperty` 返回 null 导致删除线应用异常，改用 `[Run]::`
- 测试 48 → 65 项全绿 + UI 端到端（e2e-uia.ps1）

## v1.5（2026-08-12）
- 取消开机自启恢复（`-RestoreAll` 保留参数兼容，直接退出）；启动文件夹清空

## v1.4（2026-08-12）
- WorkBuddy 审查修复（修 22）：保存失败标题红显「保存失败!」、txt/pos 原子写（修掉 Out-File 尾随换行）、反馈提示 timer 复用、Ctrl+N 新建 / Ctrl+W 关闭 / Ctrl+H 历史快捷键（标题编辑中禁用）
- 修复：心跳文件 4 进程并发注册丢 PID（FileShare.None 独占锁 + 重试）
- 测试 42 → 48 项

## v1.3（2026-08-12）
- **Ctrl+S 保存**，标题闪「已保存 ✓」1.2s
- **标题点击重命名**：点标题变输入框，回车/失焦应用、Esc 取消；校验非法字符 + 重名冲突；txt/pos 同步改名

## v1.2（2026-08-12）
- 修复：心跳 running.pids 读-改-写并发竞态（Update-PidFile 独占锁 + Mutator 回调）
- 修复：bat 文件 UTF-8 BOM 导致双击打不开（重写为 GBK 无 BOM）
- 测试 36 项

## v1.1（2026-08-12）
- **右键菜单「查看历史」**：列出所有有文字的便签，双击打开，带刷新/数据文件夹按钮
- 空便签自动删除（关闭时内容为空 → txt/pos 进回收站）
- 历史预览自动换行、保留原始换行、300 字截断

## v1.0（2026-08-11）
- 初始版：双击新建便签（可多开）、黄色小窗置顶、文本自动保存（防抖 1.5s + 兜底 30s）、位置记忆、拖动缩放
- 6 色切换（黄/绿/蓝/粉/紫/橙）、全部关闭、开机自启恢复
- 脚本自身自动滚动备份（.bak-时间戳，留最近 5 份）
